spring.application.name=apiProductos
server.port=8080

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuración de la base de datos de Railway con variables de entorno
spring.datasource.url=${SB_URL}
spring.datasource.username=${SB_USERNAME}
spring.datasource.password=${SB_PASSWORD}
